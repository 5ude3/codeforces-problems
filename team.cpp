#include <iostream>

using namespace std;

int main(){
    //function podu p v t ku loop ku list pottu
    cin << "Enter if p agrees: " << endl;
    //function podanum, loop la podanum, kela irukra logic ah
    int p, v, t;
    if((p && v && t = 1) && (p && v = 1) && (v && t = 1) (p && t = 1)){
        return 1;
    } else{
        return 0;
    }
}